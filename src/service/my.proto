syntax = "proto3";

option go_package = "/mygrpc/service";

service myServ {
    rpc sayName (Request) returns (Response);
}

message Request {
    string name = 1;
}

message Response {
    string gotName = 1;
}